# ── Credentials & tokens (NEVER commit these) ──────────────────
client_secrets.json
client_secret*.json
yt_token.pickle
*.pickle
*.pkl

# ── Python cache ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd

# ── Numba JIT cache ─────────────────────────────────────────────
*.nbi
*.nbc

# ── Virtual environments ─────────────────────────────────────────
.venv/
venv/
env/
.env

# ── OS / editor noise ────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# ── Temp / build artefacts ───────────────────────────────────────
*.tmp
*.raw
*.mp4
dist/
build/
*.egg-info/

# ── Native C build artefacts ──────────────────────────────────────
*.obj
*.dll
*.so
*.exp
*.lib
*.bat

# ── Claude Code internal ─────────────────────────────────────────
.claude/

# ── Encoder sidecar (header copy for the video description) ──────
*.sidecar

# ── Benchmark scratch output ──────────────────────────────────────
bench/*.out

# Web app scratch directory
.scratch/

# PyInstaller build output
build/
dist/
*.exe

# The file explorer's index and its cache of files fetched back from YouTube.
# The index holds every link the user has; the cache holds decrypted plaintext.
library.db
library.db-wal
library.db-shm
.cache/
